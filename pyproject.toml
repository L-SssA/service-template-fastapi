[project]
name = "service-template"
version = "0.1.0"
description = "一个 fastapi 的开发模板"
readme = "README.md"
requires-python = ">=3.10"
dependencies = [
    "fastapi>=0.123.10",
    "loguru>=0.7.3",
    "toml>=0.10.2",
    "uvicorn>=0.38.0",
    "celery>=5.3.0",
    "redis>=5.0.0",
]
